/** Half-width of attr band at confidence 0 (on 1–99 scale). */
export const ATTR_BAND_HALF_AT_ZERO = 22;

/** Base confidence gained per week on an assigned target. */
export const WEEKLY_CONF_GAIN_BASE = 12;

/** Max parallel scouting assignments (Coverage + HQ). */
export const MAX_SCOUT_SLOTS = 4;

export const DRIVER_META_KEYS = ['injuryProneness', 'longevity'] as const;
